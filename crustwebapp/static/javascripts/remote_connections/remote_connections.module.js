(function (){
    'use strict';

    angular
        .module('crust.remote_connections', [
            //'crust.remote_connections.controllers',
            'crust.remote_connections.services'
        ]);

    //angular
    //    .module('crust.remote_connections.controllers', ['ngGrid', 'ui.bootstrap']);

    angular
        .module('crust.remote_connections.services', []);

})();
